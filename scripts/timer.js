/**
 * TODO
 * Listen with active tab for youtube.com url. 
 * Decrement a timer from a set max
 * once timer is <= 0, block further redirects from the page, and show a popup
 * allow for an extension, but require a break first
 */
